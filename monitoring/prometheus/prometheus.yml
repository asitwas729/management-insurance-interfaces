rule_files:
  - /etc/prometheus/rules/*.yml
  - /etc/prometheus/rules/alert.rules.yml

global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: interfacehub
    metrics_path: /actuator/prometheus
    static_configs:
      - targets:
          - app:8080
