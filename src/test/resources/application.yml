resilience4j:
  ratelimiter:
    instances:
      loginRateLimiter:
        limit-for-period: 5
        limit-refresh-period: 1m
        timeout-duration: 0

spring:
  batch:
    job:
      enabled: false

interfacehub:
  security:
    open-endpoints-for-test: true
